import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reseau_vosges',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
